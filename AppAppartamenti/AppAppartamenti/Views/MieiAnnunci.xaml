<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:appappartamenti="clr-namespace:AppAppartamenti"
             xmlns:translator="clr-namespace:AppAppartamenti.Helpers"
             xmlns:conv="clr-namespace:AppAppartamenti.Converter" mc:Ignorable="d"
             x:Class="AppAppartamenti.Views.MieiAnnunci"
             Title="{translator:Translate MieiAnnunci.MyListing}"
              xmlns:ios="clr-namespace:Xamarin.Forms.PlatformConfiguration.iOSSpecific;assembly=Xamarin.Forms.Core" 
             ios:Page.UseSafeArea="true"
             NavigationPage.HasNavigationBar="False"
             >

   
     <ContentPage.Resources>
        <ResourceDictionary>
            <conv:ByteArrayToImageSourceConverter x:Key="ByteArrayToImage" />
            <conv:PrezzoConverter x:Key="PrezzoConverter" />

        </ResourceDictionary>
    </ContentPage.Resources>

    <ContentPage.Content>
        <StackLayout>
            <StackLayout Orientation="Horizontal">
                <Label HorizontalTextAlignment="Start" Padding="20,20,0,0" Style="{x:StaticResource Title}" Text="{translator:Translate MainPage.MyListing}"></Label>

                <Button BackgroundColor="Transparent"
                    Text="&#xf055;" HorizontalOptions="EndAndExpand" FontSize="Title" Padding="20,20,20,0"  VerticalOptions="CenterAndExpand" Style="{x:StaticResource NavBackButton}" 
                    Clicked="BtnAdd_Clicked">
                </Button>
            </StackLayout>

            <ListView x:Name="AnnunciiListView"
                    ItemsSource="{Binding Items}"
                    VerticalOptions="CenterAndExpand"
                    HasUnevenRows="true"
                    RefreshCommand="{Binding LoadItemsCommand}"
                    IsPullToRefreshEnabled="true"
                    IsRefreshing="{Binding IsBusy, Mode=OneWay}"
                    CachingStrategy="RecycleElement"
                    ItemSelected="OnItemSelected"
                    VerticalScrollBarVisibility="Never"
                    SeparatorVisibility="None">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <ViewCell>
                            <Frame HasShadow="True" Visual="Material" HeightRequest="260" Padding="0" Margin="15"  CornerRadius="2" IsClippedToBounds="True">
                                <StackLayout>
                                    <Image Aspect="AspectFill" HeightRequest="190" Source="{Binding ImmaginePrincipale, Converter={StaticResource ByteArrayToImage}}" Margin="0,0,0,150"></Image>

                                    <StackLayout Orientation="Horizontal" Margin="0,-150,0,0" Padding="10,0,0,0">
                                        <Label Text="{Binding TipologiaProprieta}" Style="{x:StaticResource Title}"  FontSize="Header"></Label>
                                        <Label Text="{Binding Prezzo}"  Style="{x:StaticResource Title}"  FontSize="Header"/>

                                        <Label VerticalTextAlignment="Center" Text="&#xf153;" FontFamily="{x:StaticResource FA-Solid}"></Label>
                                    </StackLayout>

                                     <StackLayout Orientation="Horizontal" Padding="10,0,0,0">
                                        <Label Text="{Binding NomeComune}" Style="{x:StaticResource Subtitle}" FontSize="Default" ></Label>
                                        <Label Text="-" Style="{x:StaticResource Subtitle}" FontSize="Default" ></Label>
                                        <Label Text="{Binding Indirizzo}" Style="{x:StaticResource Subtitle}" FontSize="Default" ></Label>
                                     </StackLayout>
                                </StackLayout>
                            </Frame>
                        </ViewCell>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
            </StackLayout>

    </ContentPage.Content>
</ContentPage>